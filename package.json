{
  "name": "pink-trumpet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A parameter-first, model-ready recreation of Pink Trombone.",
  "scripts": {
    "setup": "python3 -m venv .venv && .venv/bin/python -m pip install -r requirements-ml.txt && .venv/bin/python server.py --prepare",
    "dev": ".venv/bin/python server.py",
    "prepare:ml": ".venv/bin/python server.py --prepare",
    "test": "node --test",
    "compare:original": ".venv/bin/python comparison/compare.py"
  }
}
